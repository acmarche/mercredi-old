<div class="hidden-print">
    <div class="btn-toolbar" role="toolbar" style="margin: 0;">
        <div class="btn-group">
            <a href="{{ path('ecole_accompagnateur_edit', { 'id': accompagnateur.id }) }}"
               class="btn btn-primary" role="button"><i class="far fa-edit"></i> Editer</a>
        </div>
         <div class="btn-group">
            <a href="{{ path('ecole_ecole_show', { 'id': accompagnateur.ecole.id }) }}"
               class="btn btn-primary" role="button"><i class="fas fa-list"></i> Retour à l'école</a>
        </div>
    </div>
</div>