{% extends 'admin/layout.html.twig' %}

{% block body %}
    <div class="page-header">
        <h3>Edition de {{ ecole }}</h3>
    </div>
    <div class="row">
        <div class="col-md-8">
            {{ form(edit_form) }}
        </div>
    </div>

    <a href="{{ path('ecole_show', {'id': ecole.id}) }}">
        <span class="fas fa-list"></span> Annuler
    </a>

{% endblock %}
