{% if is_granted('ROLE_MERCREDI_ADMIN') %}
            <div class="btn-toolbar" role="toolbar" style="margin: 0;">
                <div class="btn-group">
                    <a href="{{ path('reduction_new') }}" class="btn btn-primary" role="button"><i class="fas fa-plus"></i> Ajouter</a>
                </div>                                 
        </div>
<br />
{% endif %}