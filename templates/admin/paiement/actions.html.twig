{% if is_granted('ROLE_MERCREDI_ADMIN') %}

    <div class="btn-toolbar" role="toolbar" style="margin: 0;">
        <div class="btn-group">
            <a href="{{ path('tuteur_show', { 'slugname': entity.tuteur.slugname }) }}" class="btn btn-primary"
               role="button"><i class="fas fa-list"></i> Retour au parent</a>
        </div>
        <div class="btn-group">
            <a href="{{ path('paiement_edit', { 'id': entity.id }) }}" class="btn btn-primary" role="button"><i
                        class="far fa-edit"></i> Editer</a>
        </div>
        {% if not entity.cloture %}
            <div class="btn-group">
                <a href="{{ path('paiement_payer', { 'id': entity.id }) }}" class="btn btn-primary" role="button"><i
                            class="far fa-credit-card"></i> Payer des présences</a>
            </div>
        {% endif %}
    </div>

    <br/>
{% endif %}