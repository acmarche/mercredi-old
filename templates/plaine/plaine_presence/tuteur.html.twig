{% extends 'admin/layout.html.twig' %}
{% form_theme form 'EnfanceMercrediBundle:Form:fields.html.twig' %}

{% block body %}

    <div class="page-header">
        <h3>Definir tuteur à <strong>{{ enfant }}</strong></h3>
    </div>

    <div class="row">
        <div class="col-lg-8">
            {{ form(form) }}
        </div>
    </div>

    <a href="{{ path('plainepresence_show_enfant', { 'enfant_slugname' : enfant.slugname, 'plaine_slugname' : plaine.slugname }) }}">
        Annuler et retour à l'enfant
    </a>

{% endblock %}